# 模拟考试功能开发计划

## 一、功能概述

模拟考试模块旨在还原真实考试场景，帮助用户检验学习成果、熟悉考试流程、提升应试能力。

---

## 二、考试模式设计

### 2.1 考试类型

| 类型 | 说明 | 题量 | 时长 |
|------|------|------|------|
| 全真模拟 | 按真实考试配比随机组卷 | 100题 | 120分钟 |
| 专项模拟 | 单科目专项测试 | 50题 | 60分钟 |
| 快速测验 | 随机抽题快速检测 | 20题 | 20分钟 |
| 错题重考 | 从错题库抽取组卷 | 30题 | 30分钟 |

### 2.2 题型配比（全真模拟）

| 题型 | 数量 | 分值 | 小计 |
|------|------|------|------|
| 单选题 | 60题 | 1分/题 | 60分 |
| 多选题 | 20题 | 1.5分/题 | 30分 |
| 判断题 | 20题 | 0.5分/题 | 10分 |
| **合计** | **100题** | - | **100分** |

---

## 三、核心功能模块

### 3.1 考试入口页 `MockExamEntryPage.ets`

**功能**：
- 考试类型选择（卡片式布局）
- 专项模拟的科目选择
- 历史考试记录入口
- 考试须知提示

**UI 设计**：
```
┌─────────────────────────────────┐
│         📝 模拟考试              │
├─────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐      │
│  │ 全真模拟 │  │ 专项模拟 │      │
│  │ 100题   │  │ 50题    │      │
│  │ 120分钟 │  │ 60分钟  │      │
│  └─────────┘  └─────────┘      │
│  ┌─────────┐  ┌─────────┐      │
│  │ 快速测验 │  │ 错题重考 │      │
│  │ 20题    │  │ 30题    │      │
│  │ 20分钟  │  │ 30分钟  │      │
│  └─────────┘  └─────────┘      │
├─────────────────────────────────┤
│  📊 历史记录 (最近5次)          │
│  ├ 12/18 全真模拟 85分          │
│  ├ 12/17 专项-混凝土 92分       │
│  └ 12/16 快速测验 80分          │
└─────────────────────────────────┘
```

### 3.2 考试答题页 `MockExamPage.ets`

**功能**：
- 顶部：倒计时 + 答题进度 + 交卷按钮
- 中部：题目内容 + 选项
- 底部：答题卡（可展开）+ 上/下题切换
- 支持标记题目（存疑）
- 支持跳转任意题目
- 自动保存答题进度
- 时间到自动交卷

**UI 设计**：
```
┌─────────────────────────────────┐
│ ⏱ 01:45:32    15/100    [交卷] │
├─────────────────────────────────┤
│ 15. [单选] 基础                 │
│                                 │
│ 水利工程质量检测的基本原则      │
│ 不包括以下哪项？                │
│                                 │
│ ○ A. 科学性原则                 │
│ ● B. 经济性原则  ← 已选         │
│ ○ C. 公正性原则                 │
│ ○ D. 及时性原则                 │
│                                 │
│ [🚩 标记]                       │
├─────────────────────────────────┤
│ [答题卡 ▼]                      │
│ ┌─┬─┬─┬─┬─┬─┬─┬─┬─┬─┐          │
│ │1│2│3│●│5│6│7│8│9│⚑│          │
│ └─┴─┴─┴─┴─┴─┴─┴─┴─┴─┘          │
│ ● 已答  ○ 未答  ⚑ 标记          │
├─────────────────────────────────┤
│    [上一题]        [下一题]     │
└─────────────────────────────────┘
```

### 3.3 考试结果页 `MockExamResultPage.ets`

**功能**：
- 成绩展示（分数、用时、正确率）
- 及格/不及格状态
- 各题型得分明细
- 各科目正确率分析
- 错题列表（可查看解析）
- 重新考试 / 返回首页

**UI 设计**：
```
┌─────────────────────────────────┐
│           考试结果              │
├─────────────────────────────────┤
│         ┌─────────┐             │
│         │   85    │             │
│         │   分    │             │
│         └─────────┘             │
│      ✅ 恭喜通过！              │
│   用时 98分钟 | 正确率 85%      │
├─────────────────────────────────┤
│ 📊 得分明细                     │
│ ├ 单选题  52/60                 │
│ ├ 多选题  24/30                 │
│ └ 判断题  9/10                  │
├─────────────────────────────────┤
│ 📈 科目分析                     │
│ 基础 ████████░░ 80%            │
│ 混凝土 ██████████ 100%          │
│ 岩土 ███████░░░ 70%            │
├─────────────────────────────────┤
│ ❌ 错题 (15题)  [查看全部 >]    │
├─────────────────────────────────┤
│  [再考一次]    [返回首页]       │
└─────────────────────────────────┘
```

### 3.4 考试记录页 `MockExamHistoryPage.ets`

**功能**：
- 历史考试记录列表
- 按时间倒序排列
- 显示考试类型、分数、用时、日期
- 点击查看详细结果
- 支持删除记录

---

## 四、数据结构设计

### 4.1 考试记录表 `exam_records`

```sql
CREATE TABLE exam_records (
  id TEXT PRIMARY KEY,
  exam_type TEXT NOT NULL,        -- full/subject/quick/wrong
  subject_id TEXT,                -- 专项模拟的科目ID
  total_count INTEGER NOT NULL,
  correct_count INTEGER NOT NULL,
  score REAL NOT NULL,
  duration INTEGER NOT NULL,      -- 实际用时（秒）
  time_limit INTEGER NOT NULL,    -- 考试时限（秒）
  create_time INTEGER NOT NULL,
  question_ids TEXT NOT NULL,     -- JSON数组
  user_answers TEXT NOT NULL,     -- JSON数组
  marked_ids TEXT                 -- 标记的题目ID，JSON数组
);
```

### 4.2 考试进度表 `exam_progress`（断点续考）

```sql
CREATE TABLE exam_progress (
  id TEXT PRIMARY KEY,
  exam_type TEXT NOT NULL,
  subject_id TEXT,
  question_ids TEXT NOT NULL,
  user_answers TEXT NOT NULL,
  marked_ids TEXT,
  current_index INTEGER DEFAULT 0,
  remaining_time INTEGER NOT NULL,
  create_time INTEGER NOT NULL,
  update_time INTEGER NOT NULL
);
```

### 4.3 TypeScript 接口

```typescript
// 考试配置
interface ExamConfig {
  type: 'full' | 'subject' | 'quick' | 'wrong';
  subjectId?: string;
  questionCount: number;
  timeLimit: number;  // 秒
  scoreRules: ScoreRule[];
}

// 评分规则
interface ScoreRule {
  questionType: QuestionType;
  score: number;
}

// 考试状态
interface ExamState {
  config: ExamConfig;
  questions: Question[];
  answers: Map<string, string>;
  markedIds: Set<string>;
  currentIndex: number;
  remainingTime: number;
  startTime: number;
}

// 考试结果
interface ExamResult {
  score: number;
  totalScore: number;
  correctCount: number;
  totalCount: number;
  duration: number;
  isPassed: boolean;
  typeScores: TypeScore[];
  subjectScores: SubjectScore[];
  wrongQuestions: Question[];
}
```

---

## 五、服务层设计

### 5.1 `MockExamService.ets`

```typescript
class MockExamService {
  // 生成试卷
  generateExam(config: ExamConfig): Promise<Question[]>
  
  // 保存进度（断点续考）
  saveProgress(state: ExamState): Promise<void>
  
  // 恢复进度
  loadProgress(): Promise<ExamState | null>
  
  // 清除进度
  clearProgress(): Promise<void>
  
  // 提交试卷
  submitExam(state: ExamState): Promise<ExamResult>
  
  // 计算成绩
  calculateScore(questions: Question[], answers: Map<string, string>): ExamResult
  
  // 保存考试记录
  saveExamRecord(result: ExamResult, state: ExamState): Promise<void>
  
  // 获取历史记录
  getExamHistory(limit?: number): Promise<ExamRecord[]>
  
  // 删除记录
  deleteExamRecord(id: string): Promise<void>
}
```

---

## 六、开发任务拆分

### 第一阶段：基础框架 ✅ 已完成

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| 1.1 | `MockExamService.ets` | 考试服务核心逻辑 | ✅ |
| 1.2 | `DatabaseService.ets` | 新增考试相关表 | ✅ |
| 1.3 | `MockExamEntryPage.ets` | 考试入口页面 | ✅ |

### 第二阶段：答题功能 ✅ 已完成

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| 2.1 | `MockExamPage.ets` | 考试答题页面 | ✅ |
| 2.2 | 倒计时组件 | 考试计时器 | ✅ |
| 2.3 | 答题卡组件 | 题目导航 | ✅ |
| 2.4 | 断点续考 | 进度保存/恢复 | ✅ |

### 第三阶段：结果分析 ✅ 已完成

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| 3.1 | `MockExamResultPage.ets` | 考试结果页面 | ✅ |
| 3.2 | 成绩计算 | 各题型/科目统计 | ✅ |
| 3.3 | 错题同步 | 自动加入错题本 | ✅ |

### 第四阶段：历史记录 ✅ 已完成

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| 4.1 | `MockExamHistoryPage.ets` | 历史记录页面 | ✅ |
| 4.2 | 记录管理 | 查看/删除 | ✅ |

### 第五阶段：主页集成 ✅ 已完成

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| 5.1 | `MainTabPage.ets` | 将"知识库"Tab改为"模拟考试" | ✅ |
| 5.2 | 路由配置 | 新增页面路由 | ✅ |

---

## 七、交互细节

### 7.1 考试流程

```
入口页 → 选择类型 → [选科目] → 开始考试 → 答题 → 交卷确认 → 结果页
                                    ↓
                              [中途退出]
                                    ↓
                              保存进度
                                    ↓
                              下次进入提示续考
```

### 7.2 交卷逻辑

- 手动交卷：弹窗确认，显示未答题数
- 时间到：自动交卷，提示"时间到"
- 中途退出：保存进度，下次可续考

### 7.3 评分规则

- 单选题：选对得满分，选错0分
- 多选题：全对得满分，少选得一半分，错选0分
- 判断题：选对得满分，选错0分

---

## 八、UI/UX 要点

1. **倒计时醒目** - 剩余10分钟变红色警示
2. **答题卡直观** - 颜色区分已答/未答/标记
3. **切题流畅** - 左右滑动切换题目
4. **进度实时** - 顶部显示当前进度
5. **结果清晰** - 分数大字居中，及格绿色/不及格红色

---

## 九、注意事项

1. **ArkTS 限制**：不使用解构赋值、不在 Builder 中声明 const
2. **性能优化**：大量题目使用 LazyForEach
3. **数据安全**：考试中定时保存进度，防止意外丢失
4. **用户体验**：交卷前二次确认，防止误操作

---

**文档版本**: v1.0  
**创建时间**: 2024-12-18
